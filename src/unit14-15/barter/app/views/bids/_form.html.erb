<%= form_for(@bid) do |f| %>
  <% if @bid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bid.errors.count, "error") %> prohibited this bid from being saved:</h2>

      <ul>
      <% @bid.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :listing_item_id %><br>
    <%= f.text_field :listing_item_id %>
  </div>
  <div class="field">
    <%= f.label :bid_item_id %><br>
    <%= f.text_field :bid_item_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
